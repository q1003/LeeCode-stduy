双指针比较灵活，可以大大降低时间复杂度，可用在数组，单链表等数据结构中。

1.快慢指针：一快一慢，步长一大一小。例如，是否有环问题（看慢指针是否能追上快指针），单链表找中间节点问题（快指针到单链表结尾，慢指针到一半）。

2.对撞指针：一左一右向中间逼近。

3.滑动窗口：类似计算机网络中的滑动窗口，一般是右端向右扩充，达到停止条件后右端不动，左端向右端逼近，逼近达到停止条件后，左端不动，右端继续扩充。

使用地方：一般来讲，当遇到需要对一个数组进行重复遍历时，可以想到使用双指针法。

  例题见leetcode
  1.移除元素
  2.删除数组中的有序项
